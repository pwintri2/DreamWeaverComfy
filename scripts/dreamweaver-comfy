#!/usr/bin/env sh
set -eu

export COMFYUI_PATH="${COMFYUI_PATH:-/home/pwintri2/ComfyUI}"
export COMFYUI_URL="${COMFYUI_URL:-http://127.0.0.1:8188}"
exec python3 /app/share/dreamweaver-comfy/server.py --host 127.0.0.1 --port "${DREAMWEAVER_PORT:-8791}" --open-browser
